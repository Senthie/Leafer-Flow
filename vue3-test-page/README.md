# Leafer-Flow Vue3 测试页面

这是一个基于Vue3的测试页面，用于验证和展示Leafer-Flow工作流编辑器的各项功能。

## 功能特性

- 🎯 完整的工作流编辑器集成
- 🎨 现代化的Vue3 + TypeScript开发体验
- 🚀 基于Vite的快速开发和构建
- 🧪 完整的测试环境（Vitest + @vue/test-utils）
- 📱 响应式设计，支持多种屏幕尺寸
- 🔧 实时状态监控和事件日志

## 开发环境要求

- Node.js >= 16
- pnpm >= 7

## 安装和运行

```bash
# 安装依赖
pnpm install

# 启动开发服务器
pnpm dev

# 构建生产版本
pnpm build

# 预览构建结果
pnpm preview
```

## 测试

```bash
# 运行测试
pnpm test

# 监听模式运行测试
pnpm test:watch

# 生成测试覆盖率报告
pnpm test:coverage
```

## 项目结构

```
vue3-test-page/
├── src/
│   ├── components/          # Vue组件
│   ├── composables/         # 组合式API
│   ├── types/              # TypeScript类型定义
│   ├── utils/              # 工具函数
│   ├── App.vue             # 主应用组件
│   ├── main.ts             # 应用入口
│   ├── style.css           # 全局样式
│   └── env.d.ts            # 环境类型声明
├── tests/                  # 测试文件
├── public/                 # 静态资源
├── index.html              # HTML模板
├── vite.config.ts          # Vite配置
├── vitest.config.ts        # Vitest配置
└── package.json            # 项目配置
```

## 开发指南

### 组件开发

所有Vue组件都使用Composition API和TypeScript开发，确保类型安全和良好的开发体验。

### 测试策略

- **单元测试**: 使用Vitest和@vue/test-utils测试组件功能
- **属性测试**: 使用fast-check进行基于属性的测试
- **集成测试**: 测试组件间的交互和数据流

### 样式规范

- 使用CSS变量定义设计系统
- 采用BEM命名规范
- 支持响应式设计
- 保持与主项目风格一致

## 浏览器支持

- Chrome >= 87
- Firefox >= 78
- Safari >= 14
- Edge >= 88

## 许可证

MIT License
